# Revisions that changed formatting and nothing else. `git blame` skips them with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically. Add a SHA here only for a commit that provably
# changed no behaviour — the swift-format adoption below was checked against the golden macro
# expansions (byte-for-byte) and the exact instruction counters (unmoved) before it landed.

# Adopt swift-format: reformat the tree to .swift-format, 202 files, whitespace only.
e630c795154d8c89be62d26f404208dfcfcb2f2d
