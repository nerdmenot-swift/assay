# Swift Package Index manifest — https://swiftpackageindex.com
version: 1
builder:
  configs:
    # AssayCore is where `Rule`, `Issue`, `Limits`, `RawValue` and `IssueCode` live; it
    # was missing until 2026-09-10, so the type a user reaches for first had no page.
    - documentation_targets: [Assay, AssayCore, AssayYAML, AssayXML, AssayTOML, AssayPlist, AssayFoundation]
