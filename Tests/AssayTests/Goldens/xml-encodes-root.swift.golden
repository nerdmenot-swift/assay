extension GoldenXML: Assay.JSONAssayable, Assay.RawDecodable, Assay.JSONEncodableSchema, Assay.RawEncodableSchema, Assay.XMLEncodableSchema, Assay.XMLRooted {nonisolated static let __assayKeyTable: [UInt8] = {
    var t = [UInt8](repeating: 3, count: 256)
        t[98] = 1
        t[105] = 0
        t[116] = 2
    return t
}()

nonisolated public static func _assay(
    from reader: inout Assay.AssayReader,
    into sink: inout Assay.IssueSink,
    at path: inout [Assay.PathComponent]
) -> GoldenXML? {
    guard reader.tryConsume(0x7B) else {
        reader.reportTypeMismatch(&sink, path, expected: "object")
        _ = reader.skipValue(&sink)
        return nil
    }
    guard reader.enterContainer(&sink) else { return nil }
    // Issues THIS decode added, not issues that exist. `sink.isValid` asks "has
    // anything ever failed?", which is wrong the moment one sink spans more than
    // one decode — a driver collecting every issue from a result set would find
    // one bad row silently discarding every good row after it.
    let __ck0 = sink.checkpoint()

    var __f0: Int? = nil
    var __f1: String? = nil
    var __f2: [String]? = nil
    var __presence: UInt64 = 0

    if !reader.tryConsume(0x7D) {
        while true {
            guard let __key = reader.scanKey() else {
                reader.reportMalformed(&sink, path, expected: "a key in double quotes")
                reader.leaveContainer()
                return nil
            }
            guard reader.expect(0x3A) else {
                reader.reportMalformed(&sink, path, expected: "':' after the key")
                reader.leaveContainer()
                return nil
            }
                let __w = reader._keyWindow(
                    __key,
                    byteOffset: 0,
                    shift: 0)
                switch Self.__assayKeyTable[Int(__w)] {
                case 0:
                    if reader.keyMatches(__key, "id") {
                        __presence |= 1
                        __f0 = reader._decodeIntCoercing(&sink, path, "id")
                    } else {
                        _ = reader.skipValue(&sink)
                    }
                case 1:
                    if reader.keyMatches(__key, "body") {
                        __presence |= 2
                        __f1 = reader._decodeStringCoercing(&sink, path, "body")
                    } else {
                        _ = reader.skipValue(&sink)
                    }
                case 2:
                    if reader.keyMatches(__key, "tags") {
                        __presence |= 4
                        if reader.tryConsume(0x5B) {
                            var __arr2_0: [String] = []
                            var __ix2_0 = 0
                            if !reader.tryConsume(0x5D) {
                                __arr2_0.reserveCapacity(reader._countArrayElements())
                                while true {
                                if let __e2_0 = reader._decodeString(&sink, path, "tags", __ix2_0) { __arr2_0.append(__e2_0) }
                                    __ix2_0 &+= 1
                                    if reader.tryConsume(0x2C) { continue }
                                    break
                                }
                                guard reader.tryConsume(0x5D) else {
                                    reader.reportMalformed(&sink, path, expected: "',' or ']'")
                                    reader.leaveContainer()
                                    return nil
                                }
                            }
                            __f2 = __arr2_0
                        } else if reader._consumeNullIfPresent() {
                            reader._nullNotAllowed(&sink, path, "tags", "array")
                        } else {
                            // Names the FIELD. This passed a bare `path` until 2026-09-08, so a
                        // whole-value mismatch on a collection reported an issue whose path
                        // was EMPTY at the top level -- worse than the missing element index
                        // this change set out to fix, and found by a test written for that.
                        reader.reportTypeMismatch(&sink, path + [.key("tags")], expected: "array")
                        _ = reader.skipValue(&sink)
                        }

                    } else {
                        _ = reader.skipValue(&sink)
                    }
                default:
                    _ = reader.skipValue(&sink)
                }
            if reader.tryConsume(0x2C) { continue }
            break
        }
        guard reader.tryConsume(0x7D) else {
            reader.reportMalformed(&sink, path, expected: "',' or '}'")
            reader.leaveContainer()
            return nil
        }
    }
    reader.leaveContainer()

        if __presence & 1 == 0 {
            reader._missingRequired(&sink, path, "id")
        }
        if __presence & 2 == 0 {
            reader._missingRequired(&sink, path, "body")
        }
        if __presence & 4 == 0 {
            reader._missingRequired(&sink, path, "tags")
        }


        guard let __v0 = __f0 else { return nil }
        guard let __v1 = __f1 else { return nil }
        guard let __v2 = __f2 else { return nil }
    let __result = GoldenXML(id: __v0, body: __v1, tags: __v2)

    guard sink.checkpoint() == __ck0 else { return nil }
    return __result
}

nonisolated public static func _assay(
    from raw: Assay.RawValue,
    into sink: inout Assay.IssueSink,
    at path: [Assay.PathComponent]
) -> GoldenXML? {
    guard case .mapping(let __members) = raw else {
        Assay.RawValue._notAnObject(&sink, path, raw)
        return nil
    }
    // See the note in the JSON body: local validity, not global.
    let __ck0 = sink.checkpoint()

    var __sp0: Assay.SourceSpan? = nil
    var __sp1: Assay.SourceSpan? = nil
    var __f0: Int? = nil
    var __f1: String? = nil
    var __f2: [String]? = nil
    var __presence: UInt64 = 0

    for __m in __members {
        let __k = __m.key
        let __v = __m.value
        switch __k.utf8.count {
            case 0:
                if __k == "" {
                    __presence |= 2
                    __sp1 = __m.span
                    __f1 = __v._assayString(&sink, path, "body", coerce: true, at: __sp1)
                } else {
                    break
                }
            case 2:
                if __k == "id" {
                    __presence |= 1
                    __sp0 = __m.span
                    __f0 = __v._assayInt(&sink, path, "id", coerce: true, at: __sp0)
                } else {
                    break
                }
            case 4:
                if __k == "body" {
                    __presence |= 2
                    __sp1 = __m.span
                    __f1 = __v._assayString(&sink, path, "body", coerce: true, at: __sp1)
                } else if __k == "tags" {
                    __presence |= 4
                    if let __r = __v.sequence.map({ $0.compactMap { __e0 in __e0._assayString(&sink, path, "tags", coerce: true) } }) {
                    __f2 = __r
                } else if case .mapping(let __wm2) = __v {
                    __f2 = __wm2.compactMap { __wmm2 in
                        let __ev2 = __wmm2.value
                        return __ev2._assayString(&sink, path, "tags", coerce: true)
                    }
                } else if __v.isNull {
                    __f2 = []
                } else if case .string(let __ws2) = __v, __ws2.isEmpty {
                    // `<tags/>` — a childless wrapper projects to empty
                    // text, and empty is exactly what it means. This is
                    // the case .wrapped exists for: absent stays absent.
                    __f2 = []
                }
                } else {
                    break
                }
            default:
            break
        }
    }

        if __presence & 1 == 0 {
            Assay.RawValue._missing(&sink, path, "id")
        }
        if __presence & 2 == 0 {
            Assay.RawValue._missing(&sink, path, "body")
        }
        if __presence & 4 == 0 {
            Assay.RawValue._missing(&sink, path, "tags")
        }


        guard let __v0 = __f0 else { return nil }
        guard let __v1 = __f1 else { return nil }
        guard let __v2 = __f2 else { return nil }
    let __result = GoldenXML(id: __v0, body: __v1, tags: __v2)

    guard sink.checkpoint() == __ck0 else { return nil }
    return __result
}

nonisolated public func _assayEncodeMembers(
    into w: inout Assay.JSONWriter,
    into sink: inout Assay.IssueSink,
    at path: [Assay.PathComponent]
) {
        w._key(encoded: "\"id\":")
        w.write(self.id)
        w._key(encoded: "\"body\":")
        w.write(self.body)
        w._key(encoded: "\"tags\":")
        w.beginArray()
        for __a2 in self.tags {
            w.write(__a2)
        }
        w.endArray()
}

nonisolated public func _assayEncode(
    into w: inout Assay.JSONWriter,
    into sink: inout Assay.IssueSink,
    at path: [Assay.PathComponent]
) {
    w.beginObject()
    self._assayEncodeMembers(into: &w, into: &sink, at: path)
    w.endObject()
}

nonisolated public func _assayEncodeRaw(
    into sink: inout Assay.IssueSink,
    at path: [Assay.PathComponent]
) -> Assay.RawValue {
    var __m: [Assay.RawValue.Member] = []
    __m.reserveCapacity(3)
        __m.append(.init(key: "id", value: .int(Int64(self.id))))
        __m.append(.init(key: "body", value: .string(self.body)))
        __m.append(.init(key: "tags", value: .sequence(self.tags.map { __e2 in .string(__e2) })))
    return .mapping(__m)
}

nonisolated public static var _assayXMLRoot: String { "r" }

nonisolated public func _assayEncodeXML(
    into w: inout Assay.XMLWriter,
    into sink: inout Assay.IssueSink,
    at path: [Assay.PathComponent],
    element __name: String
) {
    w.beginElement(__name)
        w.attribute("id", String(self.id))
        w.text(self.body)
        w.beginElement("tags")
        for __e2 in self.tags {
            w.element("tags", __e2)
        }
        w.endElement("tags")
    w.endElement(__name)
}

nonisolated public static var _assayXMLExpectedRoot: String? { "r" }}
